# =============================================================================
# 環境変数テンプレート
# =============================================================================
# このファイルをコピーして環境別の設定ファイルを作成してください:
#   - 開発環境: cp .env.example .env.dev
#   - 本番環境: cp .env.example .env.prd
# =============================================================================

# --- Application Environment ---
APP_ENV=development

# --- Django Core Settings ---
# DEBUGモード
#   開発環境 (.env.dev): True  → 詳細なエラー画面を表示
#   本番環境 (.env.prd): False → セキュリティのため非表示
DEBUG=True

# Djangoのシークレットキー（本番環境では必ず独自の値に変更してください）
# 生成方法例: python3 -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=your-secret-key-here

# アクセスを許可するホスト（カンマ区切り）
# 本番環境ではドメイン名やサーバーのIPアドレスを追記してください
ALLOWED_HOSTS=localhost,127.0.0.1

# --- Session Settings ---
# セッションバックエンドの設定
# signed_cookies: クッキーベース
# cache: キャッシュベース
SESSION_ENGINE=django.contrib.sessions.backends.xxx

# --- Database Settings (Postgres) ---
POSTGRES_ENGINE=django.db.backends.postgresql
POSTGRES_DB=pokeapp_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
POSTGRES_HOST=db
POSTGRES_PORT=5432

# --- External APIs ---
# Google Gemini API Keys (複数キーのローテーションに対応)
GEMINI_API_KEY_1=your_api_key_here
# GEMINI_API_KEY_2=
# GEMINI_API_KEY_3=
# ...